<!-- [Raw] 결정화된 ADR 영구 원장. Append-only.
     경계: *전술적* 결정(작업·ZFS 단위 ADR)만. 프로젝트 *전략적* 분기점은 project/HISTORY.md.
     전술 결정이 전략적 의미를 얻으면 HISTORY로 승격(압축). 라우팅 전체: DESIGN_RATIONALE §7. -->
# Architecture Decision Ledger
- (예시) [2026-01-01][ADR-01]: 결정화된 아키텍처 결정의 정수가 여기 1~2줄로 누적됨.
- [2026-06-14][ADR-02]: 효능 A/B 1회차(`eval/RESULTS.md`) — harness-on 3/3 vs off 1/3(+67%p). 시간축 평면(LSN·HISTORY)에서 델타 발생, 인라인 가시 계약(CON)은 델타 0. 결론: 구조 프록시는 효능 *상한*, 실현치는 지식이 *비국소*일 때 가장 큼. 근거: DESIGN_RATIONALE "두 빈칸" 중 시간축 평면이 핵심 페이오프 구간임을 실측 지지. N=1 파일럿(정식치 N=5는 후속).
- [2026-06-14][ADR-03]: v6.0 "Empirical Harness" 승격 — [PHASE-02]의 경험적 트랙(E1·E2·E4) 완료 근거. E1: 순수 비국소 측정 시 세 가치 +1.0·분산0, 모델-무관(haiku=sonnet=opus), 토큰 ROI ≈82×. E2: 100노드 평면 precision=recall=1.00·O(depth). E4: 캘리브레이션 모델(실현델타=비국소성계수×상한) E1 4점 재현. 근거: 아키텍처 주장이 *측정된 결과*로 전환됨(리뷰 필수보완 ②효능 eval 종결). 잔여: E3(enforce 도그푸딩)은 본질적 *종단*+권한 가림 작업으로 운영 트랙에 남김 — v6.0은 *경험적 증거* 확립을 표시하지 운영 단련 완료를 주장하지 않는다(정직한 경계).
- [2026-06-14][ADR-04]: 회귀 방지 원칙(E1 도출) — 시간축 평면(`reference/lessons`·`project/HISTORY`)을 "오버헤드"로 보고 제거 금지. 측정상 하네스 페이오프가 *가장 큰* 구간이 바로 비국소 지식(과거 실패·폐기 결정)이며, 그건 어떤 코드 스냅샷에도 없다. 근거: 미래 단순화 유혹에 대한 가드(eval/RESULTS.md E1 법칙).
- [2026-07-16][ADR-05]: [PRO-07] Fail-close 스코프 한정 승인·반영·검증 완주 — E5 해악 팔이 실측한 과잉 발동(H1 델타 −0.8: 전거 약한 양성 요청을 "모호함"으로 재분류)을 규칙 1 문언 한 문장("인간의 직접 요청 자체는 모호함이 아니다 — 규범·계약·HISTORY와 *충돌*할 때만 발동")으로 교정. 재측정: H1 델타 −0.8→0(5/5), T3 회귀 차단 5/5 유지(가드 무뎌짐 없음). 근거: eval/RESULTS.md E5·E5 후속. 의의: 측정→제안→승인→반영→재측정 자기진화 루프의 첫 실전 완주.
- [2026-07-24][ADR-06]: [E3] 운영 트랙 측정 완료 — ①의례 자발 수행률: union-stack 자기 레포 **0%**(편집 세션 2개 중 0), 로컬 전체 15%(27/183). 하네스 저자의 레포조차 산문 규칙만으론 의례를 수행하지 않음 → 훅(사전 강제)의 존재 이유가 자기 데이터로 확증. ②enforce 재생(hook-replay): 레포 내 122편집 중 **7건(6%) 차단, 전부 schema, lock 0**. 정탐 3(HISTORY·ARCH-00 — 실제로 승인 필요했고 승인 처리됨) 대 논쟁 4(_GUIDE 편집이 승인된 배치 중 파일 수만큼 반복 차단). 결정: **새 규칙·카브아웃 없이 enforce 전환 가능** — HOOKS.md에 이미 있는 "승인된 Schema 배치 중 mode를 warn으로 일시 하향" 운영 수칙으로 마찰 해소. 근거: eval/RESULTS.md §E3. 잔여: 라이브 훅 설치는 사용자 행위(에이전트 자동 설치 금지 불변식).
- [2026-07-24][ADR-07]: 계측기 오염 재발 — transcript-stats의 의례 판정이 임의 tool input을 스캔해 문서에 "blast-radius"를 *쓰는* Write/Edit을 수행으로 오집계(최초 100% → 수정 후 0%). 판정을 실행 표면(도구명·셸 명령·스킬 인자)으로 한정하고 회귀 3건 고정. 근거: 3회차 instrument-v2 교훈의 반복 — 자기 도메인 용어를 다루는 레포에서 문자열 매칭 계측은 항상 자기 언급에 오염된다(향후 계측기 설계 시 선점 확인 사항).
- [2026-07-24][ADR-08]: 런타임 훅을 **설치하지 않고 문서화만** 한다(기본 입장 확정). 근거: 하네스 권한 최소화 — 훅은 임의 명령 실행 + 도구 호출 거부라는 강한 권한이라, 커질수록 하네스가 새 실패 지점·마찰의 원천이 된다. 통제는 사후 게이트(permission-guard·린터) + 문서 규율 2층으로 충분히 다층이며 훅은 *선택적* 3층. 실측 뒷받침([ADR-06]): enforce 차단 7건 중 4건이 이미 승인된 배치의 반복 차단 = 강제력↑ → 마찰↑(비용은 매 세션 인간이 지불). 함의: E3의 두 측정 질문(①의례 수행률 0% ②차단 경제성 6%)은 답이 났고 라이브 전환은 *미완이 아니라 선택하지 않은 길*. 향후 세션은 "훅을 켜자"를 재제안하지 말 것 — 재검토는 마찰 비용 구조가 바뀔 때만(예: 배치 승인 단위 훅 지원).
- [2026-07-24][ADR-09]: context-budget 토큰 추정을 **스크립트별 계수**로 교체(라틴 4자/토큰, 한글·CJK·가나 1.5자/토큰). 근거: 단일 char/4는 한국어를 2~3배 과소평가해 "예산 내"를 낙관적으로 보고했다 — 문서 다수가 한국어인 이 레포에서 게이트가 사실상 무력. 즉시 효과: HANDOFF 실측 1830tok으로 예산(1500) 초과가 드러나 압축 수행(1830→674). 즉 이 게이트는 *고치자마자 실제 비대를 잡아냈다*.
- [2026-07-24][ADR-10]: 재귀 워커 7곳을 공유 유틸 `scripts/fs_walk.js`로 통일(로직 1벌·표면 N개). 불변식 둘: ①심볼릭 링크 미추적 — 자기참조 링크가 게이트를 스택 오버플로로 죽이던 구멍을 닫음(레포 관례상 평면 문서는 링크가 아니라 실파일이며 AGENTS.md도 심볼릭 대신 스텁 권장), ②Dirent 기반이라 스캔 중 사라진 항목·깨진 링크에 죽지 않음(기존 무방비 statSync 제거). 부수 발견: `zfs-linter`의 TARGET_DIRS에 `reference/tools`가 빠져 있어 TOOL 카드가 네이밍 검사를 **받지 않고 있었다**(PRO-08 등록 접점 누락) — 함께 등재. E2E: 자기참조 junction 삽입 시 기존엔 무한재귀, 수정 후 6ms 정상 종료.
- [2026-07-24][ADR-11]: MEMORY.md(에이전트 플랫폼 사적 메모리) ↔ reference/lessons 이중 저장소 경계 확정 — **범위로 가른다**: 레포/제품 특정 반복 실패 → lessons(커밋·계보 묶임·해당 계보 진입 시 주입), 환경·머신·크로스레포 사실/선호 → 사적 메모리(비커밋·전역·매 세션 주입). 근거 둘: ①누설(머신/경로/자격증명 인접 사실이 커밋 평면에 새면 안 됨), ②주입 경제(전역 사실이 한 계보에 묻히거나 계보 교훈이 매 세션 메모리를 부풀리면 안 됨). 모순 시 우선순위: 각 저장소가 자기 영역에서 authoritative, 충돌하면 더 구체적인 쪽이 이기고 다른 쪽 가지치기(둘 다 남겨 모순 방치 금지). 구현: where-to-record ROUTES에 환경 분기를 lessons *앞*에 추가(둘 다 failure/repeat 포함 → 순서가 경계). **도구 중립 유지**: 목적지를 일반명("에이전트 플랫폼 크로스세션 메모리")으로 쓰고 Claude Code MEMORY.md는 예시로만 — where-to-record의 첫 오프플레인 목적지지만 특정 플랫폼에 하드커플링하지 않음. 근거: 리뷰 P2 잔여 종결. PRO 아닌 ADR인 이유: 신규 기둥/도메인 아님(기존 도구 라우팅 1행+가이드), "게이트 아닌 가시성" 철학 정합.
- [2026-07-25][ADR-12]: 세션 종료 증류 루프 — 설계 3결정. **①신규 도메인 CNJ 신설 거부**(자체 제안 철회): 제안 계층 의미론("에이전트가 쓰되 승인 전 효력 0, 거부 사유 보존")이 추측(conjecture)과 동일해 split-principle (b) 탈락 — [PRO-04] 선례와 같은 논리. 미검증 추측은 사적 계층에 두므로 커밋 평면에 담을 것이 남지 않고, 스테이징 선례는 `spike/`(ZFS 불요·휘발성)다. 효과 0회 입증 루프를 위해 격자를 먼저 키우는 것은 반비대 규율 위반. **②미검증 후보는 사적 계층**([ADR-11] 확장, "미검증→사적 / 검증통과→커밋" 축 추가): 트랜스크립트는 비신뢰·시크릿 보유 입력(로컬 실측 1,556줄 중 75%가 절대경로; 벤더 문서상 .env 값이 그대로 기록됨). **③계측기 선행**(PRO 문서화보다): 베이스라인 없이는 반증 불가 → 포퍼 인용 자격 없음. 계측기는 루프가 반박되어도 잔존 가치가 있는 유일한 산출물. 근거: 자가생성 스킬 −1.8pp(SkillsBench 2602.12670)·자동승격 안전 0/133(2607.02579)·LLM 생성 컨텍스트 −0.5~−2%에 비용 +20%(2602.11988)·Cursor Memories 출시 후 철회. 설계 원칙: 이 루프는 생성 기계가 아니라 **제거 기계**이며, 채굴은 P₁(문제) 발견까지만 허용(귀납 금지) — 승격은 별도 추측(TT)과 생성자 아닌 검증자의 가혹한 시험(EE)을 거친다.
- [2026-07-25][ADR-13]: 마찰 계측 기준선 실측(`scripts/session-friction.js`, TOOL-16) — 이 레포 세션 2개·의도 74건. **의도당 왕복 턴 평균 17.28(중앙값 10), 출력 토큰 평균 45,719(중앙값 14,058)**. P₁ 후보 판정에 단일 지표가 틀린다는 것이 자기 데이터로 재현됨: **빈도 1위=push(18건)·비용 1위=review(1.25M tok)·동형성 1위=fix(1.00, n=8)로 셋이 전부 다름**. 그래서 판정 규칙을 "비용↑ + 도구시퀀스 동형성↑ + 독립세션≥2"로 확정 — 비용이 높아도 동형성이 낮으면(review 0.47) 매번 다른 일을 한 것이지 흡수 가능한 절차가 아니다. 부수: 동형성 수치는 표본 n을 함께 보고한다(1.00(n=4)와 1.00(n=12)는 다른 증거). 근거: 빈도 기반 채굴의 실패(utility mining)와 상관 반복의 독립 증거 오인(오승격 0.371→0.032) 대응.
- [2026-07-25][ADR-14]: 시나리오 층 착수([PRO-10]) — 작업 유형별 절차를 `kind: scenario` TOOL 카드로 카탈로그화. **신규 도메인·평면 0**(CNJ 거부와 동일 논리: 시나리오는 스킬이고 TOOL이 이미 사용 계약 형식을 강제). 핵심 설계 결정 셋: ①**내용과 라우팅을 분리 측정**한다 — 문헌상 내용은 효과(고정 단계 분해가 자율 선택을 이김, 큐레이션 스킬 +16.6pp)이고 라우팅은 실패(자동 페르소나 선택=무작위 동등, 스킬 그림자 −21%@202개, 라우팅 F1 −16~23pp@51→584)이므로 한 팔에 섞으면 실패 원인을 못 가린다. ②따라서 **`activation: manual` 기본** — 인간이 고르고 에이전트가 실행. ③**`roleDefinition` 금지**(페르소나 프레이밍은 코딩이 최악 피해 범주) + **증거 없는 Active 금지**(evidence[]가 비면 Draft 고정). 아키타입은 확립된 분류 채택(corrective/기능추가/adaptive/perfective/Development) — 사용자 원안의 "유지보수성 강화"는 리팩토링과 같은 칸이라 범위 파라미터로 흡수, "그린필드"는 ISO 12207 Development 층위임을 명시. 상한 5(2~3 최적, 4↑ 급감). 근거: eval/PROTOCOL.md §3-bis(E6), 리서치 2건.
- [2026-07-25][ADR-15]: 시나리오 층의 **반증 조건 사전 확정** — 시나리오 3개가 각각 3-arm A/B(①무시나리오 ②강제 ③자가선택)를 거쳐 **어느 것도 ①을 못 이기면 층 전체를 폐기**한다. 전례: Cursor Custom Modes 제거(2025-11-21)·Roo Code 아카이브(2026-05-15), 양쪽 사유 미공표. 첫 대상을 결함수정으로 고른 이유는 **가장 반증당하기 쉬워서**다 — 실측 동형성 1.00(n=8)로 절차는 안정적이나 Feedback-Bandwidth 가설상 효과 없음이 예보되는 자리(실패 테스트가 이미 절차를 준다). 어느 결과든 학습: 효과가 나면 가설 강화, 안 나면 "동형성보다 피드백 대역폭이 우선"이라는 경계 획득. 부수 확인: 이 가설을 이 레포가 E1 T2(인라인 가시 계약 → 델타 0)에서 8개월 먼저 관측했다.
- [2026-07-27][ADR-16]: E6 리그 확정 + 계측기 귀속 결함 수정 — 진입점 검토에서 §3-bis가 *실행 불가*임이 드러나 리그를 먼저 짓는다. **①N 정의 확정(인간 결정): 팔당 총 5런**(§0의 "각 태스크 N=5회"와 충돌했다 — 과제 3~5건이면 10런 vs 30~50런으로 3~5배 차). **②팔은 top-level 세션·팔별 격리 전사 디렉터리**([TOOL-14] worktree로 워크스페이스를 가르면 전사도 갈린다): E1~E5의 1턴 임베딩 리그는 *의도당 턴*을 낼 수 없고, 서브에이전트 팔은 sidechain 발화가 세그먼트를 열지 않아 **의도 0건 + 토큰만 부모 가산**으로 측정 자체가 불가능하다. **③과거 실측 기준선의 ① 재사용 금지** — 그 수치는 자연 작업 분포이며 작업 중인 세션이 실시간으로 분모에 들어와 *살아 움직인다*(재실행으로 확인: 17.28→16.95→17.39). 대조군은 같은 과제 스위트로 재실행한다. **④계측기 결함 수정**(`session-friction.js`): 서브에이전트 전사(`<sid>/subagents/*.jsonl`)를 파일명 기준 별개 세션으로 잡아 walk 순서에 따라 비용이 유실되거나 엉뚱한 의도에 얹혔고, 한 디렉터리에 세션이 여럿이면 다른 세션 비용이 앞 세션 마지막 의도로 샜다(A/B 팔은 런 5개가 한 디렉터리에 있으므로 치명적). 부모 sid 귀속 + 세션 내 시간순 정렬 + 세션 경계 가드로 수정. **수정 결과 [ADR-13]·[TOOL-17]의 보고치가 바뀐다: 서브에이전트 턴 234→1608, fix 버킷 동형성 1.00(n=8)→0.80(n=9)** — 기존 픽스처가 이 누수에 의존하고 있었다(테스트 2건이 수정과 함께 깨져 정정). 동형성은 애초에 시퀀스가 아니라 *도구 이름 집합의 평균 쌍별 Jaccard*이며 코퍼스 의존이다(전 프로젝트 186세션에서는 fix 0.39). [ADR-15]의 착수 근거는 "가장 반증당하기 쉬운 자리"이므로 이 하향에도 유지된다. **⑤신규 계측기 2종**: [TOOL-18] scenario-rubric(품질 비악화 = 재현 선행 ∧ 회귀 고정, 실행 표면만 관측 — [ADR-07] 준수), [TOOL-19] eval-arm(런별 비용 + Mann–Whitney U 정확 임계표 등표본 n=4~10 + 성공 바 기계 판정). **⑥과제 오염 처방**: 과거 수정 커밋 역산 결함은 정답이 히스토리에 남아 두 팔 모두 조회로 즉답 가능 → 히스토리 없는 워크스페이스나 신규 변이 주입. 남은 선행 조건은 과제 스위트 고정 하나다.
- [2026-07-27][ADR-17]: E6 과제 스위트 고정 — 이 레포 **실결함 5건**(D1 컨텍스트 예산 한국어 과소평가 `c3104e3` / D2 tools-index CRLF 거짓 드리프트 `12f5d89` / D3 blast-radius 본문 status 오인 · D4 훅의 기술 토큰 ID 오인 · D5 append-only 표 행 삭제 누락 `a11f9e0`)을 과거 수정 커밋에서 역산해 재주입한다([TOOL-20] `e6-workspace`). 합성 버그를 쓰지 않는 이유는 난이도가 인위적이어서다. **오염 차단 3겹**: ①워크스페이스는 사본+결함 주입 후 **커밋 1개짜리 새 저장소**(git 워크플로는 유지하되 수정 이력만 제거 — 히스토리를 두면 두 팔 모두 `git log` 한 번으로 즉답한다) ②결함을 *지목하는* 테스트 단언 제거(남기면 실패 메시지가 원인을 알려준다), 그 테스트가 **숨은 오라클** ③발화는 증상만(원인·파일·커밋 해시 금지 — 테스트가 강제). **빌드 검증 4항 전부 통과**: 워크스페이스 전체 테스트 체인 그린 · 증상 CLI 재현 · 결함 상태에서 오라클 실패 · 정답 복원 시 오라클 통과. 부수 결정: 결함 주입은 정확히 1회 매칭이 아니면 Fail-close(소스 드리프트 은폐 금지)하고, CJK 범위 리터럴처럼 육안 동일·코드포인트 상이 구간은 정규식 편집으로 우회한다(D1에서 실제로 걸렸다). D3는 증상이 드러나려면 픽스처 문서가 필요해 워크스페이스에 함께 주입한다(두 팔 동일 조건). 한계 명시: 5건 모두 *순수 함수 국소 결함*이라 다중 모듈·경합 결함으로 일반화 금지 — [ADR-15] 반증 판정 시 이 범위를 함께 읽어야 한다.
- [2026-08-18][ADR-18]: 재제안 차단 표식 도입·소급 등재([PRO-14] 승인) — 결정 기록의 *보존*은 이미 규율이었으나(`proposals/_GUIDE.md` Supersession principle) **배송 경로가 없었다**: upward-fetch는 PLAN/FLOW/CON/ARCH/MTG+LSN만 모으고, 부트스트랩은 원장·proposals를 포함하지 않으며, 유일한 경로가 HANDOFF(latest-only)에 손으로 옮겨 적는 목록이었다 — 한 세션이 빠뜨리면 결정이 조용히 사라진다. 해법은 온디맨드 도구가 아니라 상시 주입 인덱스다([ADR-06] 의례 자발 수행률 자기 레포 0%, [TOOL-07] "인덱스는 항상 보여야 작동"). 구현: `blocks:`/`reopen_when:` 필드 2개 + `scripts/blocks-index.js`([TOOL-22])가 AGENTS.md 마커 블록에 컴파일 + `npm run lint` 게이트(CLARIFY만, REJECT 0). 순효과는 예산 **감소** — HANDOFF §4의 목록이 인덱스로 이관되므로 단일 출처가 latest-only 파일에서 append-only 원장으로 옮겨간다. 소급 라벨링은 하지 않는다(append-only 위반 + [PRO-12] 마찰 선례); 아래 셋만 이 항목이 흡수한다.
  - [ADR-08] blocks: "런타임 훅 설치 재제안" · reopen_when: "배치 승인 단위 훅을 에이전트 플랫폼이 지원해 승인된 배치의 반복 차단이 사라질 때"
  - [ADR-12] blocks: "세션 종료 자동 증류·자동 승격 루프 재도입" · reopen_when: "생성자가 아닌 검증자의 가혹한 시험(EE)을 통과하는 승격 절차가 설계되고, 채굴이 P₁ 발견까지로 제한됨이 보장될 때"
  - [ADR-15] blocks: "시나리오 층 확대(반증 실험 전 신규 시나리오 추가)" · reopen_when: "3-arm A/B에서 어느 시나리오든 ①무시나리오 팔을 이길 때"
- [2026-08-18][ADR-19]: 계측기 자기언급 오염 **3회차** — [PRO-14] 구현 즉시 재현됐다. `blocks-index`가 제안 본문 §3의 *가공 예시* 표식(`[ADR-08] blocks: "…"`)을 실제 항목으로 집계해 ADR-08이 중복 등재됐다. [ADR-07]이 남긴 함정("자기 도메인 용어를 다루는 레포에서 문자열 매칭 계측은 항상 자기 언급에 오염된다")이 세 번째로 관측된 것이며, 이번엔 *표식 문법을 설명하는 문서가 그 문법의 소스이기도 하다*는 새 경로였다. 결정(향후 모든 계측기에 적용되는 표준): ①펜스 코드블록(```) 내부는 스캔 제외 ②설명 산문을 담는 문서 종류(proposals)는 frontmatter만 스캔 ③인라인 코드 스팬(백틱)도 제외 — ③은 *이 ADR을 쓰는 줄이 다시 오염원이 되어* 추가됐다(오염을 기록하는 행위 자체가 오염 경로다. 4회차라 부를 수도 있다). 3중 방어이며 한 겹이 뚫려도 나머지가 남는다. 회귀 5건 고정(`blocks-index.test.js`: 펜스 내부 미집계·stripFences 동작·펜스 미닫힘 시 보수적 전체 제외·인라인 스팬 제외·스팬 밖 표식 보존) + 실레포 통합 검사 2건(중복 id 0·reopen_when 전건 보유). 잔여: 이 패턴이 3회 반복됐으므로 `reference/lessons/LSN-*` 승격 후보다(현 라우팅 규칙상 2~3회 반복 함정 = LSN) — 다음 세션 판단.
- [2026-08-18][ADR-20]: 계보 판정의 **양방향 불일치** 수정(`zfs_util.isDescendant`) — 알파벳으로 끝나는 부모에서 단말 작업 접미사 `-N`을 자식으로 인정하지 않아, `ancestorChain("01a-1")`은 부모를 `01a`로 역산하는데 `isDescendant("01a","01a-1")`은 false를 반환했다. 즉 **올라가는 계산과 내려가는 계산이 서로 다른 답을 냈다**. 여파: 단말 WO가 ①blast-radius 잠금에서 누락(다른 에이전트 침범 가능 — [PRO-05] 파티션 불변식 훼손) ②`plan/_GUIDE`의 GC 조건("계보의 모든 후속이 terminal")에서 계수 누락 ③플릿 서브트리에서 탈락. 하필 `sprint/next.md`가 배송하는 예시가 `[WO-01a-1]`로 정확히 깨지는 쪽이었고, AGENTS.md 예시 `WO-01a1-2`(숫자 종료 부모)는 정상이라 육안 점검으로는 드러나지 않았다. 수정: 단말 접미사를 부모의 끝 문자와 **무관한 공통 분기**로 승격(ARCH-00 규칙 2 "A terminal task ends with -[0-9]+"가 원래 그런 뜻이다). 형제 오인 방지(01a1⊅01a10, 01a⊅01ab)는 유지. 회귀 21단언 고정 — 케이스 표 10건 + **양방향 일치 불변식**(ancestorChain이 부모라 하면 isDescendant도 자식이라 해야 한다)을 5개 ID에 대해 검사. 교훈: 역함수 쌍은 각각 테스트하면 통과하고 *함께* 테스트해야 어긋남이 드러난다 — 이 레포에 역함수 쌍이 더 있는지는 미점검.
- [2026-08-18][ADR-21]: [PRO-15] 승인·구현 — 작업 종료 의례 신설. 진입 3중(upward-fetch·check-prereqs·blast-radius) 대 종료 0의 비대칭을 닫았다. **결정적 병목은 GC 교착이었다**: `plan/_GUIDE`의 GC 조건("계보의 모든 후속이 terminal")이 WO 상태에 의존하는데 WO에 상태가 없어 판정 불가였고, 그래서 PLAN은 원리적으로 Crystallized에 닿을 수 없었다(PLAN-01이 아직 Draft인 이유). 구현 셋: ①WO 문서 정본화(frontmatter status·evidence·closed_by, 상한 1,500 tok — [PRO-13]의 "예산 초과=라우팅 실패" 원리) ②`sprint/next.md`를 **생성 뷰**로 전환(tools-index·blocks-index와 같은 컴파일 패턴 — 종료 시 행 제거가 절차가 아니라 부수효과가 된다) ③`work-close`([TOOL-23]) — 닫힘을 선언이 아니라 **확인 가능한 사실 둘**(상위 축 흔적 1곳 + 증거 명시)로 정의하고, `closed_by`가 가리킨 파일에 실제 계보 참조가 있는지 검사해 "선언만 하고 반영 안 함"을 잡는다. 증거의 `none — 이유` 허용은 [PRO-13]의 "해당 없음" 구별을, 증거 요구 자체는 smell-linter의 `unevidenced-active` 판정을 다른 도메인에 이식한 것이다(새 원리 0). REJECT 0 — 작업 종료 시점의 차단은 [PRO-13]이 이미 기각했고 같은 논리로 "불완전하게 닫힌 WO가 안 닫힌 WO보다 낫다". Closed WO는 **세션 종료 시** `sprint/archived/`로 배치 이동하며 **삭제하지 않는다**(색인에 남아야 GC 계수가 성립 — 보관이 곧 기능이다). 회귀 28단언(`work-close.test.js`).
  - [ADR-21] blocks: "feature/live.md 의 Status 열 어휘 정의" · reopen_when: "work-close 가 행의 존재가 아니라 값을 봐야 하는 요구가 실제로 생길 때"
- [2026-08-18][ADR-22]: 잠금 어휘에서 `Live` **제거**([PRO-15] §7). 코드(`query.js`·`health.js`의 LOCKED)는 `Live`를 잠갔으나 어느 가이드에도 정의가 없었고, 의도한 출처로 보이는 `feature/live.md`는 `zfs_index.SCAN_DIRS`에 없어 색인조차 되지 않았다 — **발동 경로가 원천적으로 없는 안전장치**였다. 실측: 50노드 중 Verifying/Live 0건(한 번도 발동한 적 없음). 정의를 부여하지 않고 제거한 근거는 [ADR-08]의 기준이다 — 강제력↑ → 마찰↑, 비용은 매 세션 인간이 낸다. 필요가 입증되지 않은 잠금을 새로 정의하는 것은 그 기준을 통과하지 못하며 [ADR-12]의 "제거 기계" 규율에도 어긋난다. 실제 용도(플릿 침범 방지 — [PRO-05])는 `Verifying` 하나로 충분하다. 범위: 코드 6곳 + 규범 문서 5곳. **과거 기록은 고치지 않았다** — [PRO-05]·[PRO-06]·`eval/RESULTS.md`·`DESIGN_RATIONALE` §7의 표기는 그 시점의 사실이며 Supersession 원칙상 [PRO-15]가 대체한다(삭제가 아니라 대체). 교훈: 있다고 믿는 안전장치가 없는 것보다 위험하다.
  - [ADR-22] blocks: "Live 상태값 재도입" · reopen_when: "운영 중인 산출물을 보호해야 했는데 못 한 사례가 실제로 관측될 때"
- [2026-08-18][ADR-23]: 효과 표면 **관측** 1절 신설(`health.js` `effect surface`) — 갭 분석 §3-C의 hold 출구를 최소 조치로 닫는다. 실측 근거: 이 레포의 비가역 외부 효과는 `.claude/settings.local.json`의 **allow 92건**(Bash 41 · WebFetch 40 · PowerShell 6 · 그 외 5, wildcard 13, deny·ask 0)이 다스리는데, 이 목록은 4계층 권한·`permission-guard`·`ARCH-00` 어디에도 없다 — 등급도 근거 기록도 게이트도 없는 유일한 통제면이었다. **새 게이트가 아니라 INFO 1절**인 이유: [ADR-08]이 기각한 것은 훅에 의한 *강제*였지 *관측*이 아니었고, 강제는 마찰 증거가 먼저다([PRO-11] §4 측도 금지 — 이 절은 판정·점수를 만들지 않는다). 부수 결정: 도구 이름을 목록으로 박지 않고 **규칙에서 도출**한다 — `Bash`만 세는 첫 구현이 `PowerShell` 6건과 `WebFetch` 40건을 조용히 누락했다(계측기가 자기 가정으로 눈머는 [ADR-07]과 같은 병리). 설정 파일 부재는 0이 아니라 "관측 불가"로 표기한다. 다음 판단점: wildcard 비율이나 deny/ask 0이 문제로 관측되면 그때 게이트를 제안한다.
- [2026-08-18][ADR-24]: `ref-linter` known set에 **행 정본**(원장 `[ADR-N]` 실행 앵커 + `GRANTS.md` `GRANT-N` 행) 편입 — 미해소 계수 114→53. ADR 64건+GRANT 2건은 파일이 아니라 행이라 **구조적으로 영원히 미해소**였고, ADR을 쓸 때마다 계수가 늘었다(112→114 자기 실측). 갈림길에서 **제외가 아니라 편입**을 택했다: 접두사 제외는 유령 참조(ADR-99 같은 오타)를 영원히 통과시키지만, 앵커 편입은 실재하는 행만 해소하므로 오타 검출력을 *얻는다*. 경계 두 가지: ①**buildIndex에는 넣지 않는다** — ADR-16의 id "16"이 색인에 들어가면 계보 16을 오염시켜 [ADR-25]의 앨리어싱을 역수입한다(known set은 ref-linter 지역). ②앵커만 뽑고 본문 속 언급은 제외한다 — 언급까지 넣으면 유령이 스스로를 해소한다. 예시 행(`- (예시) [..][ADR-01]`)은 GRANT-90과 같은 취급으로 앵커가 아니다. 계측기가 자기 가정(파일만 실존)으로 눈멀던 [ADR-07]·[ADR-23] 병리 클래스의 세 번째 수리.
- [2026-08-18][ADR-25]: ID 공간 **단일 유지 명문화**(ARCH-00 §ID space) — 계수 도메인(TOOL·PRO)의 좌표 앨리어싱(`blast-radius TOOL-10`≡`PRO-10` 실측)은 알려진 트레이드오프로 수용하고 코드 분리는 하지 않는다. 근거 셋: ①도메인 단위 규칙은 진짜 결합(PRO-10→WO-10a-1, 의도된 에지)과 가짜 결합(TOOL-10, 잡음)을 못 가른다 — 판별자는 의도이고, 의도를 코드화하면 ID 산술(하네스 핵심 테제)이 선언 에지 모델로 대체된다. ②실피해 0(오탐 조건인 앨리어스 계보 내 `Verifying`이 0)이고 `health.js` lock exposure가 상시 관측 중. ③되돌림 비용 비대칭 — 명문화 후 수리는 싸고(사례가 근거), 선제 분리 후 번복은 계보 산술 전반의 테스트·의미론을 두 번 뒤집는다. [ADR-08]·[ADR-23]과 같은 문법: 강제·수리 전에 관측.
  - [ADR-25] blocks: "계수 도메인의 계보 ID 공간 분리(코드 수정)" · reopen_when: "잠금 오탐 또는 GC 오차단이 실제 관측될 때"
- [2026-08-20][ADR-26]: 대시보드 뷰 라우팅을 **JS → CSS로 이전**(라디오 `:checked ~` 형제 선택자, 진입점 7개 전부 `<label>`) — 스크립트를 실행하지 않는 뷰어에서 축 페이지에 **도달 불가**였다. [TOOL-25] 카드는 "클릭이 정본, 해시는 부가"라 적었으나 **클릭 핸들러도 JS다**: 관측된 증상(해시 미유지)의 원인을 해시로 오귀속해, 같은 결함을 두 번 고쳤다(1차: 전 뷰 `hidden` → 빈 화면. 2차: 클릭 핸들러 → 전환 불가). 판별 질문은 "무엇이 전환을 *수행*하는가"이지 "무엇이 전환을 *지시*하는가"가 아니다. 내구성 3단으로 재설계: JS+CSS → 기존 동작 + 해시 북마크 / JS 죽음 → **전환 성립**(CSS) / CSS까지 죽음 → 전 뷰가 쌓여 내용은 읽힘. 검증은 단언이 아니라 **스크립트 제거 사본의 실클릭**으로 했다(단언은 1차 수리도 통과시켰다 — 계측기가 자기 가정으로 눈멀던 [ADR-07]·[ADR-23]·[ADR-24] 병리의 네 번째 사례이며, 여기서 눈먼 가정은 "테스트 환경 = 배송 환경"이다). 어답터 실사용 보고에서 발견 — 상류 자기 사용만으로는 브라우저에서만 열어 보므로 영원히 안 보인다.
