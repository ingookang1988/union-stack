---
description: List docs — the ZFS document index, optionally filtered by domain (e.g. PLAN, FLOW)
allowed-tools: Bash(node scripts/query-cli.js:*)
---
Document index (optional domain filter `$ARGUMENTS`):

!`node scripts/query-cli.js list_docs $ARGUMENTS`
